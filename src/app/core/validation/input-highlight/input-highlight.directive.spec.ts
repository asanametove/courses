import { InputHighlightDirective } from './input-highlight.directive';

describe('InputHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new InputHighlightDirective(
      {} as any,
      {} as any,
      {} as any,
    );
    expect(directive).toBeTruthy();
  });
});
